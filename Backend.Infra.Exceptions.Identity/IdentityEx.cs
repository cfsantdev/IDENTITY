namespace Backend.Infra.Exceptions.Identity
{
    public static class IdentityEx
    {
        public const string SUPPORT_EMAIL = "cfs.plus.s8@gmail.com";

        public static readonly SessionEx Session = new SessionEx(SUPPORT_EMAIL);
        public static readonly SessionEx Session = new SessionEx(SUPPORT_EMAIL);
    }
}