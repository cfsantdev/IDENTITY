namespace Backend.Infra.Enumerators
{
    public class Mapper
    {
        public readonly Type EnmPatternsMap = typeof(FetchByNamePattern);
        public readonly Type EnmRolesMap = typeof(Roles);
    }
}